# Проверка обновления AAA URBAN

Дата: 16 сентября 2026 года.

## Выполнено

- `npm test`: production-сборка Vinext завершена успешно; 12 тестов пройдены.
- `npx tsc --noEmit --incremental false`: ошибок TypeScript нет.
- Production Worker возвращает статус 200 для `/`, `/kz`, `/privacy`, `/kz/privacy`, `/robots.txt`, `/sitemap.xml`.
- Язык серверного HTML — `ru` или `kk`; заголовки и основной текст доступны до запуска JavaScript.
- Внутренние ссылки указывают на существующие секции, идентификаторы элементов не повторяются.
- Все изображения, указанные в HTML, существуют локально; у изображений есть `alt`.
- Основные WhatsApp-ссылки ведут на сохранённый номер `77012200112`, сообщения локализованы.
- Canonical, языковые альтернативы, sitemap и контакты Schema.org проверены.
- Старый API заявок возвращает 410 без обращения к D1 и без ложного сообщения об успехе.

## Границы проверки

Браузерная визуальная проверка и полный сценарий заполнения формы с кликами не выполнены. Производительность Core Web Vitals не измерялась. Сообщения в WhatsApp не отправлялись.

Публикация в аккаунте Cloudflare заказчика не выполнялась. Архив нужно загрузить в существующий репозиторий. В README указаны настройки сборки и порядок проверки после публикации.

Фотографии реальной команды и новые кейсы ЖК ещё не предоставлены. Механизм их добавления готов, неподтверждённые данные на сайте не выводятся. Битрикс, GA4 и GTM без данных заказчика не подключены.
